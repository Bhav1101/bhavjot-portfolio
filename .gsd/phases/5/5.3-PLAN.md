---
phase: 5
plan: 3
wave: 3
depends_on:
  - 5.1
  - 5.2
gap_closure: true
---

# Plan 5.3: Add Missing Budget Field and Complete Verification

## Objective
Address the gaps identified during Phase 5 verification: Add the missing "budget" dropdown field to the Contact form, update the submission state accordingly, and finally wait for the user to visually verify the frontend aesthetics and animations.

## Context
Load these files for context:
- client/src/components/sections/Contact.jsx

## Tasks

<task type="auto">
  <name>Add Budget Field to Contact Form</name>
  <files>
    client/src/components/sections/Contact.jsx
  </files>
  <action>
    Add a 'budget' dropdown to the Contact form component.
    
    Steps:
    1. Update the formData initial state in Contact.jsx to include budget: ''.
    2. Add a select dropdown for the budget field (e.g., options like "$0 - $1,000", "$1,000 - $5,000", "$5,000+").
    3. Ensure the dropdown uses the same styling as the other inputs and its value is bound to formData.budget via the handleChange function.
    4. Ensure the budget is included when setFormData resets the form on successful submission.
  </action>
  <verify>
    cat client/src/components/sections/Contact.jsx | Select-String "budget"
  </verify>
  <done>
    The budget field is correctly added to state and rendered as a select input in the form.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Visual Verification</name>
  <files>
    client/src/App.jsx
  </files>
  <action>
    Ask the user to run the dev servers, view the frontend at http://localhost:5173, and confirm the styling and animations match expectations, and the contact form (now with the budget field) works.
    
    Steps:
    1. Direct the user to start the client and server.
    2. Ask the user to visually inspect the scrolling animations and submit a test message in the contact form.
  </action>
  <verify>
    echo "Wait for user confirmation"
  </verify>
  <done>
    User confirms the site looks and acts correctly.
  </done>
</task>

## Must-Haves
After all tasks complete, verify:
- [ ] Contact form UI includes name, email, budget, and message fields.
- [ ] User visually verifies the application aesthetics.

## Success Criteria
- [ ] All tasks verified passing
- [ ] Must-haves confirmed
- [ ] No regressions in tests
