---
phase: 5
plan: 2
wave: 2
depends_on:
  - 5.1
gap_closure: false
---

# Plan 5.2: Framer Motion Animations and Final Verification

## Objective
Apply Framer Motion animations across all sections to emulate the reference site (`sawad.framer.website`), refining layout rhythms and colors. Finally, request human verification of the design.

## Context
Load these files for context:
- .gsd/SPEC.md
- client/package.json
- client/src/components/sections/*.jsx

## Tasks

<task type="auto">
  <name>Add Framer Motion Animations to Sections</name>
  <files>
    client/src/components/sections/Hero.jsx
    client/src/components/sections/Stats.jsx
    client/src/components/sections/Skills.jsx
    client/src/components/sections/Experience.jsx
    client/src/components/sections/Projects.jsx
    client/src/components/sections/Certifications.jsx
    client/src/components/sections/Education.jsx
    client/src/components/sections/Contact.jsx
  </files>
  <action>
    Integrate `framer-motion` for scroll-based entrance animations across all sections.
    
    Steps:
    1. Ensure `framer-motion` is installed in `client/package.json`.
    2. In each component, import `motion` from `framer-motion`.
    3. Replace standard HTML tags with `motion.*` tags (e.g., `<motion.section>`, `<motion.div>`) for the main wrappers.
    4. Apply `initial`, `whileInView`, `viewport={{ once: true, amount: 0.2 }}`, and `transition` props for smooth slide-up and fade-in effects (e.g., `initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.5 }}`).
    5. Polish spacing and Tailwind classes across components for visual consistency.
    
    AVOID: Overly complex or jarring animations.
    USE: Subtle, professional fade and slide-up animations consistent with modern portfolio sites.
  </action>
  <verify>
    grep -r "framer-motion" client/src/components/sections/
  </verify>
  <done>
    All section components utilize framer-motion `whileInView` for animations.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Visual Verification</name>
  <files>
    client/src/App.jsx
  </files>
  <action>
    Ask the user to run the dev servers, view the frontend at `http://localhost:5173`, and confirm the styling and animations match expectations, and the contact form works.
    
    Steps:
    1. Direct the user to start the client and server.
    2. Ask the user to visually inspect the scrolling animations and submit a test message in the contact form.
  </action>
  <verify>
    echo "Wait for user confirmation"
  </verify>
  <done>
    User confirms the site looks and acts correctly.
  </done>
</task>

## Must-Haves
After all tasks complete, verify:
- [ ] Smooth scroll animations are present via Framer Motion.
- [ ] User visually verifies the application aesthetics.

## Success Criteria
- [ ] All tasks verified passing
- [ ] Must-haves confirmed
- [ ] No regressions in tests
